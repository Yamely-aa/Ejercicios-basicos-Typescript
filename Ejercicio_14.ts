//Define un enum `Color` con valores `Red`, `Green`, y `Blue`. Declara una variable de tipo `Color` y asígnale un valor.

enum Color {
    Red = "Rojo",
    Green = "Verde",
    Blue = "Azul"
}

let colorFavorito: Color = Color.Green;
