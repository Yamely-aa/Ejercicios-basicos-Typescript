//Crea una función que reciba un objeto `Person` y retorne un string con el nombre y edad de la persona

function describirPersona(persona: Person): string {
    return `${persona.name} tiene ${persona.age} años.`;
}
